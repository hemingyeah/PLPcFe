let settingProps = {
  field: {
    type: Object,
    default: () => ({})
  },
  setting: {
    type: Object,
    default: () => ({})
  }
}

let previewProps = {
  field: {
    type: Object,
    default: () => ({})
  },
  setting: {
    type: Object,
    default: () => ({})
  }
}

export {
  settingProps,
  previewProps
}