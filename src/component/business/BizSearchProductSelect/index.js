import component from './BizSearchProductSelect.vue';

const BizSearchProductSelect = {
  install(Vue){
    Vue.component(component.name, component);
  },
  component
}

export default BizSearchProductSelect;