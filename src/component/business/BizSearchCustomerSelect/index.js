import component from './BizSearchCustomerSelect.vue';

const BizSearchCustomerSelect = {
  install(Vue){
    Vue.component(component.name, component);
  },
  component
}

export default BizSearchCustomerSelect;