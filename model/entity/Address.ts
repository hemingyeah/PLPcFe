interface Address {
  // 详细地址
  address ?: String;
  // 城市
  city ?: String;
  // 区域
  dist ?: String;
  // id
  id ?: String;
  // 纬度
  latitude ?: String;
  // 经度
  longitude ?: String;
  // 省份
  province ?: String;
}

export default Address